<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>All users:</h1>
    <ul>
        <% users.forEach((user, i) => { %>
            <li><a href="/profil/<%= i %>"><%= user.name %></a></li>
        <% }); %>
    </ul>
</body>
</html>